<p>Page not found.</p>
{% if suggested_link %}
    <p>Perhaps, this <a href="{{ suggested_link }}">page</a> can be of use to you?</p>
{% endif %}
{% if (not 'httperror/404' in url) or req404 %}
    <code>{{ req404|default(url) }}</code>
{% endif %}
<img loading="lazy" decoding="async" alt="Not found" src="/img/errors/missing.svg">
