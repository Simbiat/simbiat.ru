<blockquotev data-author="Server">HTTP method <i>{{ http_method }}</i> is not supported.</blockquotev>
{% if reason %}<samp class="failure" data-description="Detailed reason">{{ reason }}</samp>{% endif %}
<img loading="lazy" decoding="async" alt="Bad method" src="/img/errors/wrongtool.gif">
