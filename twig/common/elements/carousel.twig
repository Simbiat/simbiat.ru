<div class="imageCarousel">
    <div class="imageCarouselPrev">❰</div>
    <ul>
        {% for image in images %}
            {%  if image.href %}
                <li>
                    <figure>
                        <a class="galleryZoom" href="{{ image.href }}" target="_blank">
                            <img loading="lazy" decoding="async" src="{% if image.thumb %}{{ image.thumb }}{% else %}{{ image.href }}{% endif %}" alt="{% if image.alt %}{{ image.alt }}{% else %}{{ image.href|basename }}{% endif %}">
                            <div>{{ loop.index }}/{{ images|length }}</div>
                        </a>
                        <figcaption>{% if image.caption %}{{ image.caption|raw }}{% else %}{% if image.alt %}{{ image.alt }}{% else %}{{ image.href|basename }}{% endif %}{% endif %}</figcaption>
                    </figure>
                </li>
            {% endif %}
        {% endfor %}
    </ul>
    <div class="imageCarouselNext">❱</div>
</div>
