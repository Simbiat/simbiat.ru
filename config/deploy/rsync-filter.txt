# File to apply rsync filter rules

# Format:
# CWRULE [PATTERN_OR_FILENAME]
# CWRULE,MODIFIERS [PATTERN_OR_FILENAME]

# Rules:
# `exclude`, `-` specifies an exclude pattern (if used with `--delete-excluded`, will behave like `hide`).
# `include`, `+` specifies an include pattern.
# `merge`, `.` specifies a merge-file to read for more rules.
# `dir-merge`, `:` specifies a per-directory merge-file.
# `hide`, `H` specifies a pattern for hiding files from the transfer (behave as if the file does not exist on the source, thus removing it on target).
# `show`, `S` files that match the pattern are not hidden.
# `protect`, `P` specifies a pattern for protecting files from deletion (prevents deletion on target).
# `risk`, `R` files that match the pattern are not protected.
# `clear`, `!` clears the current include/exclude list (takes no arg)

# Need to use `/` at the start of entries to anchor rsync exlusions/inclusions.
# If it's missing, will match anything that has this kind of entry (either at the end or anywhere in the path).
# `./` breaks the rules completely, so should not be used.

H /.git/
H /.github/
H /.idea/
+ /assets/
- /assets/vendor/
+ /bin/
H /bin/backup-and-mirror.sh
H /bin/deploy.sh
H /bin/phpunit
+ /build/
H /build/BIC/
H /build/DDL/
H /build/js/dev/
H /build/PSDs/
H /build/install.sh
H /build/toupdateonprod.sql
H /build/useful.cmd
+ /config/
H /config/crowdsec/online_api_credentials.example.yaml
H /config/deploy/
H /config/eslint.config.mts
H /config/php_dev.ini
H /config/phpcs.xml
H /config/phpstan.neon
H /config/phpunit.xml
H /config/psalm.xml
H /config/stylelint.config.ts
+ /data/
+ /data/.gitignore
- /data/security.json
+ /data/backups/.gitignore
# Can have subfolders
- /data/backups/**
+ /data/ffstatistics/.gitignore
- /data/ffstatistics/*
+ /data/mariadb/.gitignore
- /data/mariadb/**
P /data/mariadb/**
+ /data/temp/.gitignore
# Has subfolders
- /data/temp/**
+ /data/uploaded/.gitignore
# Has subfolders
- /data/uploaded/**
P /data/uploaded/**
+ /data/uploadedimages/.gitignore
# Has subfolders
- /data/uploadedimages/**
P /data/uploadedimages/**
+ /migrations/
H /node_modules/
+ /packages/
#TODO: will likely need to be updated once Symfony will start handling items through the `assets/` folder
+ /public/
P /public/assets/images/fftracker/crests-components/**
P /public/assets/images/fftracker/icons/**
+ /src/
+ /templates/
H /tests/
+ /translations/
+ /var/
+ /var/.gitignore
- /var/cache/prod/
H /var/cache/dev/
H /var/cache/test/
H /var/deploy/
+ /var/log/.gitignore
- /var/log/*
+ /var/mergedcrests/.gitignore
# Has subfolders
- /var/mergedcrests/**
- /var/share/prod/
H /var/share/dev/
H /var/share/test/
+ /var/sitemap/.gitignore
# Has subfolders
- /var/sitemap/**
- /vendor/
H /.aiignore
H /.editorconfig
+ /.env
H /.env.dev
H /.env.test
- /.env.local.php
P /.env.local.php
H /example.env
H /.gitattributes
H /.gitignore
H /.gitmodules
H /bun.lock
+ /.compose.yaml
H /.compose.override.yaml
H /.compose.predeploy.yaml
+ /.composer.json
+ /.composer.lock
+ /.importmap.php
H /LICENSE
H /package.json
H /README.md
H /SECURITY.md
+ /symfony.lock
H /TODO.md
H /tsconfig.json
