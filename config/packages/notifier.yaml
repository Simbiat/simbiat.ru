framework:
    notifier:
        chatter_transports:
        texter_transports:
        channel_policy:
            # use chat/Slack, chat/telegram, sms/twilio or sms/nexmo
            urgent: [ 'email' ]
            high: [ 'email' ]
            medium: [ 'email' ]
            low: [ 'email' ]
        admin_recipients:
            - { email: admin@simbiat.eu }
