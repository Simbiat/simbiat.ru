{% if json_ready %}
    {# Twig will escape quotes and other characters without `raw` filter here, even if Twig's `|json_encode` filter is used #}
    {{ json_ready|raw }}
{% else %}
{
    "status": {{ http_error }},
    "CSRF": "{{ session_data.csrf }}",
    "reason": "{% if reason %}{% if reason == 'Teapot' %}Unsupported client{% else %}{{ reason }}{% endif %}{% else %}Unknown{% endif %}"
}
{% endif %}
