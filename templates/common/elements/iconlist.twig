<ul class="icon_list">
    {% for icon in icons %}
        <li>
            <figure>
                <a class="gallery_zoom no_new_tab_icon" href="{{ icon.icon }}" target="_blank">
                    <img crossorigin="anonymous" loading="lazy" decoding="async" src="{{ icon.icon }}" alt="{{ icon.name|default(basename(icon.icon)) }}" width="48" height="48">
                </a>
                <figcaption class="middle">{{ icon.name|default(basename(icon.icon)) }}</figcaption>
            </figure>
        </li>
    {% endfor %}
</ul>
