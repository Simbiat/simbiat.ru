<article class="margin_05rem">
    <details>
        <summary class="right_summary about_section">{% if service_name == 'bictracker' %}Помощь по поиску{% else %}Search help{% endif %}</summary>
        {% if service_name == 'bictracker' %}
            <p>Поиск вначале осуществляется для точных совпадений, для ускорения выдачи по точным ID или именам. Если точных совпадений не найдено, то будет осуществлён полнотекстовый поиск.</p>
            <p>Вы можете использовать следующие контрольные символы для управления выдачей:</p>
            <ul>
                <li><var>+</var> перед словом делает его обязательным (иначе слова считаются <i>опциональными</i>)</li>
                <li><var>-</var> перед словом исключает результаты с ним из выборки</li>
                <li><var>&gt;</var> перед словом увеличивает вес результатов с ним</li>
                <li><var>&lt;</var> перед словом уменьшает вес результатов с ним</li>
                <li><var>~</var> перед словом сделает вес результатов с ним <i>отрицательным</i>, опуская их в выборке, но не исключая как <var>-</var></li>
                <li><b>*</b> после слова позволяет искать все результирующие слова, <i>начинающиеся</i> с него</li>
                <li><var>&quot;</var> вокруг слова или фразы позволят осуществить строгий поиск, без применения возможных естественных изменений слов</li>
                <li><var>(&nbsp;)</var> позволяют группировать слова для создания сложных условий поиска</li>
            </ul>
            <p>Если вам всё равно не удаётся найти нужное, попробуйте использовать <var>%</var> символ для неточного поиска, но имейте в виду, что такой поиск может занять значительное время.</p>
        {% elseif service_name == 'fftracker' %}
            <p>Search is first done for the exact match of the value to provide faster results for exact IDs or names. If no match is found, full text search is done searching for the whole word.</p>
            <p>You can try using these control symbols to adjust search:</p>
            <ul>
                <li><var>+</var> before a word will make it required (otherwise words are considered <i>optional</i>)</li>
                <li><var>-</var> before a word will exclude all matches containing it</li>
                <li><var>&gt;</var> before a word will increase the relevance of results containing it</li>
                <li><var>&lt;</var> before a word will decrease the relevance of results containing it</li>
                <li><var>~</var> before a word will make the relevance of results containing it <i>negative</i>, pushing them to the bottom, but not excluding like <var>-</var></li>
                <li><var>*</var> after a word will allow searching for resulting words that <i>start</i> from it</li>
                <li><var>&quot;</var> enclosing a word or a phrase will search for it literally without any possible natural search conditions applied to it</li>
                <li><var>(&nbsp;)</var> allow you to group words for more complex conditions</li>
            </ul>
            <p>If you still do not find what you are looking for, you can try adding a <var>%</var> symbol for a fuzzy search, but note that then search can take quite a long while.</p>
        {% endif %}
    </details>
</article>
