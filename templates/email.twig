{% apply inline_css(source('@styles/email.css')) %}
    <!doctype html>
    <html lang="en" style="height: 100% !important;">
        <head>
            <meta charset="UTF-8">
            <meta http-equiv="Content-Type" content="text/html; charset=UTF-8">
            <meta name="viewport" content="width=device-width, initial-scale=1.0">
            <title>{{ subject }}</title>
            <meta name="description" content="{{ og_desc|default('Official Simbiat Software website') }}">
        </head>
        <body style="height: 100% !important;">
            <div id="content" style="height: 100% !important;">
                <div id="header">
                    <div id="header_mail">
                        <div class="header_text">Simbiat</div>
                        <div id="logo_nav">
                            <img crossorigin="anonymous" loading="lazy" decoding="async" alt="logo" id="logo_icon" class="cursor_pointer" data-tooltip="Simbiat Software" src="{{ email.image('@images/favicons/favicon-32x32.png') }}" width="32" height="32">
                        </div>
                        <div class="header_text">Software</div>
                    </div>
                    <div id="h1_div_mail">
                        <h1 id="h1_title">{{ subject }}</h1>
                    </div>
                </div>
                <div id="main" style="height: 100% !important; width: 100% !important;">
                    <div class="article margin_05rem">
                        <p>Hi, {{ username|default('dear user') }}!</p>
                        {# This can be a valid HTML generated in respective notification class, and that is sanitized before we get here #}
                        {{ text|raw }}
                    </div>
                </div>
                <div id="footer">
                    <div>
                        <a href="{{ domain }}/about/contacts">Contact us</a>
                        {% if unsubscribe_all %} or
                            <a href="{{ domain }}/uc/unsubscribe?unsubscribe_all={{ unsubscribe_all|url_encode }}">unsubscribe from all emails</a>
                        {% endif %}
                    </div>
                    <div class="xx_small">
                        Email generated at {{ timeTag(created, 'Y-m-d H:i:s') }} UTC and sent at {{ timeTag(sent, 'Y-m-d H:i:s') }} UTC
                        {% if tracker %}
                            <!--suppress HtmlUnknownTarget -- This is a dynamic PHP path -->
                            <img crossorigin="anonymous" loading="lazy" decoding="async" class="read_status" src="{{ domain }}/api/uc/notifications?uuid={{ tracker }}" alt="Read status" title="Read status" width="32" height="32">
                        {% endif %}
                    </div>
                </div>
            </div>
        </body>
    </html>
{% endapply %}