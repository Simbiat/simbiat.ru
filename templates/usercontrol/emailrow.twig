<tr class="middle">
    <td>{{ email.email|default('email') }}</td>
    <td>
        {% if not email or email.activation %}<input type="button" value="Confirm" class="mail_activation" data-email="{{ email.email }}"><img loading="lazy" decoding="async" class="hidden spinner inline" src="/assets/images/spinner.svg" alt="Activating {{ email.email }}...">{% else %}<span class="mail_confirmed">✅</span>{% endif %}
    </td>
    <td{% if not email or email.activation %} class="warning"{% endif %}>{% if not email or email.activation %}Confirm address to change setting{% else %}<span class="float_label"><input id="subscription_checkbox_{{ number }}" class="mail_subscription" data-email="{{ email.email }}" type="checkbox"{% if email.subscribed %} checked{% endif %}{% if email.subscribed %} disabled{% endif %}><label for="subscription_checkbox_{{ number }}"{% if email.subscribed %} data-tooltip="Select another email to unsubscribe this one"{% endif %}></label></span><img loading="lazy" decoding="async" class="hidden spinner inline" src="/assets/images/spinner.svg" alt="Changing subscription for {{ email.email }}...">{% endif %}</td>
    <td>
        <input class="mail_deletion" data-email="{{ email.email }}" type="image" src="/assets/images/close.svg" {% if email and ((email.activation and count_activated == 0) or (not email.activation and count_activated == 1) or (not email.activation and email.subscribed and count_subscribed == 1)) %}disabled alt="Can't delete the only confirmed or the only subscribed email" {% else %}alt="Delete {{ email.email|default('email') }}"{% endif %}><img class="hidden spinner inline" src="/assets/images/spinner.svg" alt="Removing {{ email.email|default('email') }}..." loading="lazy" decoding="async">
    </td>
</tr>
