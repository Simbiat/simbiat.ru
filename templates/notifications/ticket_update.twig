<p>Your ticket <var>{{ ticket }}</var> has a new post.</p>
<p>To view the ticket and update it, if required, just click 👉 <a href="{{ domain }}/talks/threads/{{ token }}" rel="noopener noreferrer">here</a> 👈</p>
<p class="warning">Note, that the link above is the only link you can use to access the ticket, unless you register, and it gets updated with every post. We recommend keeping this email for future tracking of progress.</p>
